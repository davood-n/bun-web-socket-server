{
  "dependencies": {
    "bun-types": "^0.2.2"
  },
  "scripts": {
    "start": "bun run ./src/entry-point.ts",
    "dev": "nodemon -L",
    "code-count": "cd src && git ls-files | xargs cat | wc -l"
  }
}